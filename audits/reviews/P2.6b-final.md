# P2.6b final closure review (P26bFinal)

> This document is edited and maintained by Claude and presented as-is.
> Final read-only closure review of the P2.6b tower after the N1 repair:
> `Atlas/Proofs/IsometryConvergence.lean` deleted, its sole theorem inlined into
> `Atlas/Proofs/SecondQuantization.lean`. Date: 2026-08-24. No reviewed file edited.

## Findings

### N2 (P3) — `Atlas/Proofs/ShellOneParticle.lean:58`: "shared" is stale after the inlining

The docstring reads "The shared dense-set extension theorem
`Isometry.tendsto_of_dense` is not needed." When that line was written the theorem
lived in the shared module `IsometryConvergence.lean`; after the N1 repair it is
declared inside `SecondQuantization.lean` (`ThreeEpsilon` section) with that one
module as its only consumer. The factual content ("not needed") stays true; the
adjective "shared" now points at a home that no longer exists. Drop the word or
name the owner. Non-blocking.

## Verdict

**PROOF-READY / FREEZE-READY. Confidence 0.95.**

The N1 repair is faithful: the shallow module is gone, the theorem lives once in
its only consumer, and nothing else moved. All seven findings of
`audits/reviews/P2.6b.md` and the N1 finding of `audits/reviews/P2.6b-rereview.md`
remain closed against the current tree. The proof tower and the modified witness
build green; every P2.6b module recompiles file-level with zero errors and zero
warnings; the committed probe keeps its 17 standard-base axiom pins; the citation,
orphan, and frozen-closure gates all pass. The only defect is the P3 one-word
docstring nit above.

## N1 closure — ownership of `Isometry.tendsto_of_dense`

* `Atlas/Proofs/IsometryConvergence.lean` does not exist (glob). The name survives
  only inside `audits/reviews/P2.6b-rereview.md`, the historical record of the
  state it reviewed; no live code, script, manifest, or doc references it (grep
  over the tree; `BLUEPRINT.md`, `PROGRESS.md`, `docs/` clean).
* `Atlas/Proofs.lean` registers every P2.6b module (plus the P2.6c KG modules)
  and does not import the deleted module (git diff of the registration file).
* The theorem is declared once, at `Atlas/Proofs/SecondQuantization.lean:122`
  (`ThreeEpsilon` section, lines 113-141), and used exactly twice, both in that
  file: `tendsto_tensorPowerCongr` (:873, dense algebraic image of the completed
  power) and `tendsto_secondQuantization` (:897, frozen dense finite-particle
  subspace). The module docstring's "used twice" (:50) matches.
* Negative closure pin: `#print axioms Isometry.tendsto_of_dense` through
  `import Atlas.Proofs.ShellPoincareRepresentation` fails with `Unknown constant`.
  The shell tower does not see the Fock-side lemma, so the P2.6b lane and the
  P2.6a lane stay decoupled.
* The two remaining prose mentions are name-only and accurate:
  `ShellOneParticle.lean:58` ("not needed" — modulo N2) and
  `ShellPoincareRepresentation.lean:79` ("not used").

## LpPullback — minimal consumed single-equivalence primitive

179 lines: the `Construction` section (two round-trip cancellation lemmas, the
primitive `Lp.measurePreservingEquiv` at :100, and its identity, composition,
inverse, norm, and representative formulas) plus the headline `Pinning` example
(:174). The `AutGroup`/`AutHom`/`StrongContinuity` sections, `equiv_symm`, and
every ε/3 remnant stay deleted; `MeasurePreservingAut`, `pullbackHom`,
`tendsto_of_dense_simpleFunc`, and `spatialProj` match nothing live. Inverse legs
use Mathlib's `MeasurePreserving.symm` (`he.symm e`: :103-107, :120, :133, :160).

Sole importer: `ShellOneParticle.lean:2` (grep over `Atlas`, `CandidateLab`,
`scripts`, `audits`). Consumption: `shellPoincare` (:640-646, the pullback leg of
the Wigner operator), `coeFn_shellPoincare` (:654-662, via
`Lp.coeFn_measurePreservingEquiv`), `shellPoincare_translation` (:695-707, via
`Lp.measurePreservingEquiv_refl`); the downstream tower
(`MassShellMeasurePreserving`, `ShellPoincareRepresentation`) consumes it through
`shellPoincare`. The unit-multiplier layer stays ShellOneParticle-owned, matching
the docstring's scope split ("representation laws and continuity live with each
geometric action").

## Prior findings — all closed in the current tree

* **F1** — `ShellOneParticle.lean:73-79` states the H1 Jacobian identity in
  physical momentum (`|det D(shellMap m Λ)(p)| · physicalEnergy m p =
  physicalEnergy m (shellMap m Λ p)`) and points to the `MassShellInvariance`
  proof and the `MassShellMeasurePreserving` promotion. No frequency-form
  identity anywhere live (grep).
* **F2** — `MassShellMeasure.lean:359-364` keeps the honest support rationale
  ("no current consumer needs it; for positive mass the sheet is closed").
* **F3** — `equiv_symm` matches nothing live; call sites use `he.symm e`.
* **F4** — the dead generic layer stays deleted (sweep above).
* **F5** — `PoincareTopology.lean:1-3` imports exactly the three trim-test
  modules.
* **F6** — `spatialProj` matches nothing live; `ShellPoincareRepresentation`
  consumes `spatialOf` (:64).
* **F7** — `fourierMinkowski_shift` (:349) takes `{f : M4 → ℂ} (a p : M4)`, no
  integrability hypothesis, unconditional proof.

## Gates and axiom surface

* Forbidden tokens (`sorry|admit|native_decide|axiom|unsafe`): none in the eight
  recompiled modules or the committed probe.
* `scripts/check_citation_flags.py`: OK, 8 tracked flags. The three
  `SecondQuantization` entries are content-hashed, so the inlining's line shifts
  do not touch them; no new flag phrase entered any module.
* `scripts/check_no_orphans.py`: OK, 56 sources reachable — the deletion left no
  dangling import and no orphan.
* `scripts/check_frozen_closure.py`: OK (18 spec-closure files, 37
  witness-closure files, 6 frozen-by-import); `scripts/witness-closure.txt`
  carries the `PoincareTopology.lean` line and nothing stale.
* Axiom pins, all `[propext, Classical.choice, Quot.sound]`:
  `Isometry.tendsto_of_dense`, `QFT.tendsto_secondQuantization`,
  `QFT.continuous_secondQuantization` (through
  `Atlas.Proofs.SecondQuantization`); `QFT.KleinGordon.shellPoincareRep`,
  `QFT.KleinGordon.shellPoincareUnitary_mul`,
  `MeasureTheory.Lp.measurePreservingEquiv`,
  `QFT.KleinGordon.lorentzShellPreserves` (through
  `Atlas.Proofs.ShellPoincareRepresentation`); and all 17 committed-probe pins.

## Commands and results

```text
$ lake build Atlas.Proofs Atlas.Witnesses.PoincareRep
    # Build completed successfully (3139 jobs)
$ for f in PoincareTopology LpPullback MassShellMeasure MassShellInvariance \
    ShellOneParticle MassShellMeasurePreserving ShellPoincareRepresentation \
    SecondQuantization; do lake env lean Atlas/Proofs/$f.lean; done
    # all eight: exit 0, zero errors, zero warnings
$ lake env lean audits/probes/P2.6b/mass_shell_measure_probe.lean
    # exit 0; 17/17 #print axioms = [propext, Classical.choice, Quot.sound]
$ lake env lean /tmp/p26b_final/axioms_sq.lean       # 3 pins, standard base
$ lake env lean /tmp/p26b_final/axioms_shell.lean    # 4 pins, standard base
$ lake env lean /tmp/p26b_final/negative_closure.lean
    # expected failure: Unknown constant `Isometry.tendsto_of_dense`
$ python3 scripts/check_citation_flags.py   # OK, 8 flags tracked
$ python3 scripts/check_no_orphans.py       # OK, 56 files reachable
$ python3 scripts/check_frozen_closure.py   # OK, 18 + 37 + 6
```

`glob '**/IsometryConvergence*'`: no matches. `grep IsometryConvergence` outside
`audits/reviews/`: no matches. `grep` for `equiv_symm`, `MeasurePreservingAut`,
`pullbackHom`, `tendsto_of_dense_simpleFunc`, `spatialProj` over `Atlas`,
`scripts`, `audits/probes`, `BLUEPRINT.md`, `PROGRESS.md`, `docs`: no matches.
