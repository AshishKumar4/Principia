# P2.6c L0–L3 Fable re-review: repaired cone probe and dossier DAG

> This document is edited and maintained by Claude and presented as-is.
> Fable re-review of the repaired P2.6c evidence layer, delivered 2026-08-24.
> Scope: closure of D1 and D2 from `audits/reviews/P2.6c-L0-L3.md`, plus
> regression checks on all four proof modules and both committed probes.

## Findings

None. Both defects from the first review are closed. I found no regression.

## Verdict

**PROOF-READY (Fable side, confidence 0.95).** The repair touched exactly the two
prescribed artifacts — the committed cone probe and the dossier DAG — and no proof
module. L0–L3 remain sound foundations for the L4 dispatch. The Codex cross-model
pass recommended by the first review is still pending; it was not a Fable-side
blocker then and is not one now.

## Repair verification

1. **D1 (P2), vacuous PJ.2b probe point: CLOSED.** The first review proved the old
   observables at `(t, x) = (3, 0)` were flat `0 ≤ 0` checks and prescribed the
   fix: re-point at a transition time, or add a strict observable. The repair does
   both.
   - `probe_chi_transition_value`
     (`audits/probes/P2.6c/kg_cone_cutoff_probe.lean:125-135`) proves the strict
     band membership `0 < χ(11/8, 0) < 1` at the committed parameters
     `x₀ = 0, R = 2, δ = 1/2, ε = 1/4`, via exactly the two lemmas the first
     review proved reachable (`Real.smoothTransition.pos_of_pos`,
     `lt_one_of_lt_one`). The prescribed value triple is now fully committed:
     `χ(0,0) = 1` (line 77), `0 < χ(11/8,0) < 1` (line 125), `χ(3,0) = 0`
     (line 107).
   - `probe_dt_nonpos` (line 138) and `probe_cone_inequality_final` (line 146)
     are re-pointed from `t = 3` to `t = 11/8`.
   - My scratch probe re-derives the point independently: the `smoothTransition`
     argument at `(11/8, 0)` is exactly `1/2`, dead centre of the open band
     (`fable2_arg_half`), and the strict membership compiles from first
     principles (`fable2_transition_strict`).
   - The vacuity attack is dead. My scratch `fable2_gradient_center_zero` proves
     `∇χ(11/8, 0) = 0` (the `psi_delta` gradient vanishes at `x = x₀`), so the
     re-pointed cone inequality reads exactly `0 ≤ -∂ₜχ(11/8, 0)`. In the open
     band `∂ₜχ = -ε⁻¹·N′(1/2) < 0`, so the reversed inequality
     `‖∇χ‖ ≤ +∂ₜχ` — which the first review compiled at the old point — is
     false at the new point. A wrong-sign variant of `norm_gradient_chi_le` can
     no longer satisfy this probe shape. (`deriv`-level strictness stays out of
     reach in the pinned Mathlib, as the first review recorded; the strictness
     now lives at the value level, which is what the prescription asked.)
   - Axiom audit of the three repaired observables: each depends on exactly
     `[propext, Classical.choice, Quot.sound]`.

2. **D2 (P3), dossier DAG status drift: CLOSED.**
   `docs/dossiers/P2-wightman-design.md` now marks L1 "landed through PJ.1e,
   2026-08-24" (lines 134-138) with the honest remainder — API
   consolidation/upstreaming only, no missing consumer theorem — and L3 "landed
   2026-08-24" (lines 143-146) with its two stated deliverables. L0 (2026-08-23)
   and L2 (2026-08-24) are unchanged; L4–L6 correctly remain open nodes. The
   pin-inventory name `PiLp.volume_preserving_ofLp` is retained, as the first
   review already cleared it. An L4 dispatcher reading the dossier now sees the
   true frontier.

## Regression checks

- **No proof module changed since the first review.** The four-module build is a
  no-op replay (all 3069 jobs cached, wall time under two seconds), and every
  line anchor the first review cited resolves to the same declaration today:
  `pjChar` at `Atlas/Proofs/KGPropagator.lean:133`, `kgPropagator_dtdx_comm` at
  1151, `kleinGordon_kgPropagator` at 1317, `fourier_inversion_eq` at 1330;
  `integral_piDivergence_eq_zero` at `Atlas/Proofs/KGIntegrationByParts.lean:140`,
  `integral_mul_divergence` at 346; `norm_gradient_chi_le` at
  `Atlas/Proofs/KGConeCutoff.lean:389`, `deriv_chi_time` at 284,
  `chi_eq_one_of_dist_le` at 263, `support_chi_subset_closedBall` at 237;
  `energy` at `Atlas/Proofs/KleinGordon.lean:292`, `energy_eq_zero_iff` at 310,
  `onShell_inFutureTimeCone` at 234. The four modules stay wired in
  `Atlas/Proofs.lean:14-17`.
- **The unchanged probe surface survives.** `probe_chi_interior_center`
  (line 77), `probe_chi_outside_time` (line 107), the unsmoothed-distance
  refutation, and the `chiWrong` refutation are all still present; the
  propagator probe is untouched and recompiles.
- **The gate consumes the repair.** Gate 9 (`scripts/check_probes.py:15`)
  recompiles every `audits/probes/**/*.lean` by rglob, so the repaired probe is
  exercised on every gate run.
- One nit, non-blocking: the probe file header's positive-check list does not
  name `probe_chi_transition_value`. The theorem's own docstring is precise.

## Commands and results

All from the repo root, all exit `0`:

```text
$ lake build Atlas.Proofs.KleinGordon Atlas.Proofs.KGPropagator \
    Atlas.Proofs.KGConeCutoff Atlas.Proofs.KGIntegrationByParts
Build completed successfully (3069 jobs).
$ lake env lean audits/probes/P2.6c/kg_cone_cutoff_probe.lean   # exit 0, no output
$ lake env lean audits/probes/P2.6c/kg_propagator_probe.lean    # exit 0, no output
$ lake env lean /tmp/p26c_rr/rereview_probe.lean                # exit 0, no output
$ lake env lean /tmp/p26c_rr/probe_axioms.lean
'QFT.KleinGordonCutoffProbe.probe_chi_transition_value' depends on axioms: [propext, Classical.choice, Quot.sound]
'QFT.KleinGordonCutoffProbe.probe_dt_nonpos' depends on axioms: [propext, Classical.choice, Quot.sound]
'QFT.KleinGordonCutoffProbe.probe_cone_inequality_final' depends on axioms: [propext, Classical.choice, Quot.sound]
```

Scratch probe contents (`/tmp/p26c_rr/rereview_probe.lean`): `fable2_psi_center`
(`ψ(0) = 1/2`), `fable2_arg_half` (argument `= 1/2` at the probe point),
`fable2_transition_strict` (independent `0 < χ(11/8,0) < 1`),
`fable2_gradient_center_zero` (`∇χ(11/8,0) = 0`). The axiom file is the
committed probe plus three `#print axioms` lines.
