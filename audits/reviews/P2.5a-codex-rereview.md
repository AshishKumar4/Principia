# P2.5a Codex re-review verdict: repaired Wightman utilities

> This document is edited and maintained by Claude and presented as-is.
> Independent GPT-5.6-sol re-review, Codex CLI 0.147.0, delivered 2026-08-24.
> Read-only session `01a03246-e3c2-7410-9030-cf5cbaa1bb82` reviewed the repair
> diff after the four defects in `P2.5a-codex.md`.

**Verdict: FREEZE-READY (confidence 0.99).** No new defect or regression was
found. This pass independently discharges the Codex cross-model blocker, subject
to the repaired package also passing its updated Fable review and repository gates.

## Finding closure

1. **Poincaré Schwartz action:** closed. The action computes
   `f (Λ⁻¹ (x-a)) = f (g⁻¹ • x)`, satisfies
   `action (g₁*g₂) = action g₁ ∘ action g₂`, and packages `g⁻¹` as the CLE
   inverse. The mixed translation/rotation probe rejects both the forward-affine
   impostor and reversed composition order; pure translations are not doing the
   evidentiary work.
2. **Periodic sign probe:** closed. The repaired probe explicitly records
   `fourierChar 1 = fourierChar (-1)`, then proves
   `fourierChar (1/4) = I`, `fourierChar (-1/4) = -I`, and inequality at the
   character level. It also proves forward-cone membership, `E=2πp⁰>0`, and
   `fourierChar (t*p⁰)=exp(+iEt)` against the frozen Stone convention.
3. **Fourier attribution:** closed. Mathlib supplies the `exp(-2πi<x,p>)`
   convention. Reed–Simon II §IX.1 is now correctly described as using the
   symmetric `(2π)^(-n/2) exp(-iλ·x)` convention. Wightman 2000 pp. 210–211
   supplies the `exp(+ip·ξ)` reconstruction with forward-cone support.
4. **Bochner completeness:** closed. `[CompleteSpace H]` is now in
   `integrable_smul_of_bounded`; its only caller compiles.

## Checks run by Codex

- Compiled the Poincaré spec, both Wightman modules, all five P2.5a probes, and
  the root Specs/Witnesses/Atlas aggregators.
- Axiom audit passed: 1,654 Atlas declarations.
- Witness audit passed: 571 declarations.
- Frozen closure passed: 18 spec / 36 witness files.
- Targeted forbidden-token scan and `git diff --check` passed.

No P2.5a finding remains.
