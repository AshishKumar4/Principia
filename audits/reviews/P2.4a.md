# P2.4a review verdict — Minkowski extraction + Poincaré groups

> This document is edited and maintained by Claude and presented as-is.
> Fable adversarial review of commit 2c16b5d, delivered 2026-08-10.
> First Workflow-v2-compliant review: probe evidence committed in
> `audits/probes/P2.4a/` (three files, each compiles via `lake env lean`).

**Verdict: FREEZE — approve with two conditions (both applied in the freeze
commit).** No frozen statement could be broken. All five gates reproduced
independently by the reviewer, including lean4checker over 38 modules.

## Findings

1. **Extraction fidelity: PASS, mechanical.** 13 moved declarations byte-identical
   in statement AND proof between 74892ff and 2c16b5d (two docstring-only cross-ref
   updates, disclosed); 48 retained witness declarations byte-unchanged; no
   renames/losses/duplicates; new spec content verified genuinely new.
2. **IsSpacelike strictness: correct.** Strict form matches S&W spacelike
   separation; the causal anchor mechanically forces it (reflexivity + null edge
   both kernel-probed); divergence from P1.1 `Spacelike` (O'Neill zero-vector
   convention) documented and un-confusable.
3. **Anchor, orthochronous closure (mul and inv), Poincaré group law, MulAction
   twist, mass shells (incl. m = 0 puncture edges): all probed in the kernel** —
   see `anchor_causal_probe.lean`, `massshell_poincare_probe.lean`,
   `rot12_member_probe.lean` (the last constructs diag(1,−1,−1,1) and proves all
   three membership conditions through the frozen det spelling — the P2.4W
   obligation is de-risked to ~180 lines of adaptation).

## Conditions applied at freeze

- **D1 (medium):** the causal anchor lives in `Atlas/Witnesses/MinkowskiCausal.lean`,
  outside every mechanical guard → both anchor-bearing witness files added to
  `scripts/frozen-imports.txt` (hook-guarded by name from this commit).
- **D2 (low):** explicit P2.4W non-vacuity gate line added to BLUEPRINT (P2.4b must
  not consume the group spec before a nontrivial member is witnessed).
- **D3 (nit):** provenance wording precision ("moved verbatim (code; two docstring
  cross-references updated)").
- D4 (info, no change): `massShell m = massShell (−m)` — matches physics usage.
