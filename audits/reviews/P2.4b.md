# P2.4b review verdict — strongly continuous Poincaré representations

> This document is edited and maintained by Claude and presented as-is.
> Fable adversarial review of commit ec64ffc, delivered 2026-08-10.
> Probe evidence: four files in `audits/probes/P2.4b/`, each compiling
> warning-free via `lake env lean`.

**Verdict: FREEZE — approve with three conditions (all doc/process; zero frozen
statements changed).** All five gates reproduced independently against the HEAD
surface (the working tree carried unrelated concurrent P2.6a work at review time;
the reviewer correctly isolated it).

## Decisive findings

1. **The character-rep obstruction (kernel-certified).** The tasked cheap
   nontrivial witness U(a,Λ) = e^{i⟪a,k⟫}·1 is mathematically impossible for
   k ≠ 0: the MonoidHom law against the frozen semidirect product forces Lorentz
   invariance of the exponent functional, and L↑₊ fixes no nonzero functional —
   Wigner's reason nontrivial reps are infinite-dimensional. Turned into
   `character_obstruction_probe.lean`: kernel proof that NO MonoidHom of character
   form exists (defect exhibited at rotation12 × translation, closed via exp
   periodicity). Certifies the MonoidHom field has real teeth.
2. **Cross-lane Stone probe** (`stone_bridge_probe.lean`): trivialRep's
   translationGroup proven EQUAL to the P2.3g trivialGroup witness; its Stone
   generator computed = 0 two independent ways (transport, and directly via
   generator_apply_of_hasDerivAt). The P2.4b anchor and the P2.3g generator
   machinery verify each other with no shared intermediate.
3. **Topology non-degeneracy** (`topology_nondegeneracy_probe.lean`): the
   component map is an embedding; PoincareGroup proven T2; {1} proven not open
   (discrete reading excluded via the injective continuous translation line);
   indiscrete reading excluded. Joint continuity genuinely constrains orbits.
4. **API/coercions** (`api_coercion_probe.lean`): U g⁻¹ = star (U g) reachable
   both at unitary and operator level; no coercion diamond (all rfl);
   `noncomputable` on FunLike is compiler-required (codegen), kernel-irrelevant.

## Conditions applied at freeze

- **D1 (low, book-gated):** S&W section pins deferred to physical-book check —
  web sources conflict on section offsets; owner action recorded (also flags a
  likely docstring errata on P2.4a's "§1-1").
- **D2 (medium):** BLUEPRINT gate added — downstream PROOF-consumption of
  PoincareRep is blocked until the nontrivial regular-rep witness on L²(M4)
  lands (trivial rep is provably the only cheap model); P2.5b may state against
  the structure.
- **D3 (low):** joint-continuity adjudication docstring now owns the decision
  (the dossier never adjudicated joint vs per-vector; S&W is the source).
- D4/D5 (info): noncomputable marker required; private clm_one_apply duplicate
  disclosed as upstream candidate.
